<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Update User</title>
    <style>
        .flag {
            width: 20px;
            height: 15px;
            margin-right: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Update User</h2>
        <form action="/user/update/<%= user._id %>" method="POST">
            <div class="mb-3">
                <label for="name" class="form-label">Name</label>
                <input type="text" class="form-control" id="name" name="name" value="<%= user.name %>" required>
            </div>
            <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input type="email" class="form-control" id="email" name="email" value="<%= user.email %>" required>
            </div>
            <div class="mb-3">
                <label for="phonenumber" class="form-label">Phone Number</label>
                <div class="input-group">
                    <select class="form-select" id="countryCode" name="countryCode" required>
                        <option value="" disabled>Select country</option>
                        <option value="1" data-country="US">🇺🇸 +1</option>
                        <option value="44" data-country="GB">🇬🇧 +44</option>
                        <option value="91" data-country="IN">🇮🇳 +91</option>
                        <option value="81" data-country="JP">🇯🇵 +81</option>
                        <option value="84" data-country="JP">vn +84</option>
                        <!-- Add more countries and codes as needed -->
                    </select>
                    <input type="tel" class="form-control" id="phonenumber" name="phonenumber" placeholder="Phone Number" value="<%= user.phonenumber.toString().slice(3) %>" required>
                </div>
                <small class="form-text text-muted">Select your country code and enter your phone number.</small>
            </div>
            <div class="mb-3">
                <label for="role" class="form-label">Role</label>
                <input type="text" class="form-control" id="role" name="role" value="<%= user.role %>" required>
            </div>
            <button type="submit" class="btn btn-primary">Update User</button>
            <a href="/userlist" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</body>
</html>
